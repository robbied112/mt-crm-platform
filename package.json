{
  "name": "crufolio",
  "private": true,
  "scripts": {
    "setup": "cd frontend && (test -f .env || cp .env.example .env) && (test -d node_modules || npm install)",
    "dev": "npm --prefix frontend run dev -- --host 0.0.0.0 --port ${CONDUCTOR_PORT:-5173}",
    "build": "npm --prefix frontend run build",
    "test": "npm --prefix frontend run test"
  }
}
